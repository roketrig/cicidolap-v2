# Cloudflare Worker'ı deploy ettikten sonra çıktıda görünen adresi buraya
# yazın ve bu dosyanın adını ".env" olarak değiştirin (ya da ".env.example"ı
# kopyalayıp ".env" oluşturun). Vite sadece "VITE_" ile başlayan
# değişkenleri tarayıcı koduna dahil eder.
VITE_IMAGE_WORKER_URL=https://cicidolap-images.SIZIN-HESABINIZ.workers.dev
